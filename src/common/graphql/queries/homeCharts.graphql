query HomeCharts($userId: String!, $ledgerId: String) {
  homeCharts(userId: $userId, ledgerId: $ledgerId) {
    data {
      type
      label
      data {
        date
        balance
        budgets
      }
    }
    success
  }
}
